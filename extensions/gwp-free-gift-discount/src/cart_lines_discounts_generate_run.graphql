query CartInput {
  cart {
    lines {
      id
      quantity
      attribute(key: "_gwp_free") {
        value
      }
      cost {
        subtotalAmount {
          amount
        }
      }
    }
  }
  discount {
    discountClasses
  }
}
